name: PR Validation CI

on:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Terraform validate
        working-directory: prod
        run: terraform validate

      - name: Dummy step
        run: echo "CI is running on Pull Request"
